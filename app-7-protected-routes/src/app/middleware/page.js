export default function Middleware() {
  return (
    <h1 style={{ margin: "20px", textAlign: "center", padding: "40px" }}>
      Middleware
    </h1>
  );
}

// look at middleware.js inside src folder
